{
    "name": "Python 3",
    "build": {
        "dockerfile": "Dockerfile",
        "context": "."
    },
    "postCreateCommand": "python -m venv .venv && pip install -r requirements.txt",
    "customizations": {
        "vscode": {
            "extensions": [
                "tomoki1207.pdf"
            ]
        }
    }
}